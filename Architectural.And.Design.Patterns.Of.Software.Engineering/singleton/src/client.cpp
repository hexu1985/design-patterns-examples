#include "tool_tip_manager.h"

int main(int argc, char *argv[])
{
    ToolTipManager::getInstance().operation();
    ToolTipManager::getInstance().operation();
    ToolTipManager::getInstance().operation();

    return 0;
}

