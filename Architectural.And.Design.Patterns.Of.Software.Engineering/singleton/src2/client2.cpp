#include "tool_tip_manager2.h"

int main(int argc, char *argv[])
{
    ToolTipManager2::getInstance().operation();
    ToolTipManager2::getInstance().operation();
    ToolTipManager2::getInstance().operation();

    return 0;
}

